general:
  buildTool: 'mta'
steps:
  mtaBuild:
    buildTarget: 'CF'
    platform: 'CF'
  cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
      apiEndpoint: 'https://api.cf.us10.hana.ondemand.com'
      org: 'PreSales_jmr'
      space: 'dev'
      credentialsId: 'cf-deploy'
